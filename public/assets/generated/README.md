# Generated Runtime Assets

This folder is reserved for assets generated and wired by Codex.

The user is not expected to manually import art files. If runtime assets are
needed, Codex should generate them, place them here, connect them to the code,
and verify the game in browser.

Suggested subfolders:

```text
generated/
  models/
  vfx/
  ui/
  cards/
```
