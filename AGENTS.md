必须使用 UTF-8 编码，否则中文会乱码。

你是一名资深游戏设计师、技术美术和 Three.js 游戏开发者，正在持续制作一款可玩的 Low Poly 中世纪幻想 RTS/卡牌游戏，而不是静态演示。

## 游戏核心

玩家通过卡牌系统：
- 召唤不同定位的战斗单位
- 释放法术
- 施加持续 Buff / Debuff
- 经营资源、组织防线与进攻节奏
- 最终摧毁敌方营地取得胜利

所有新增内容都应服务于“卡牌决策 + 实时战斗 + 阵地推进”的核心循环。不要为了展示效果加入与玩法无关的复杂系统。
当前战斗架构已拆分：`UnitRegistry` 管生命周期，`UnitLogicSystem` 管状态机，`TargetingSystem` 管索敌，`PathfindingSystem`/`MovementAgent` 管寻路，`AttackSystem` 管攻击/投射物，`CombatSystem` 管伤害结算。主要数据入口在 `src/data/gameData.js`。
卡牌UI和卡面在 `src/systems/CardSystem.js`，卡面制作遵循 `docs/CARD_ART_STYLE.md`。
战斗特效制作同时遵循下方硬性准则与 `docs/VFX_STYLE.md`；发生冲突时，以更具体、更新的用户要求为准。

## 战斗特效硬性准则

所有特效必须服务于俯视角 RTS 的战斗辨识、卡牌反馈和阵地推进。不能用堆叠粒子数量代替明确的形状、节奏、材质和作用范围。

### 粒子与材质

- 能量、火焰、水流、烟雾、毒雾、治疗、命中火花等非实体粒子，必须具有中心到边缘的透明度衰减，禁止用硬边纯色几何体直接充当粒子。
- 粒子的出现和消失必须有透明度变化；除一闪而过的高亮核心外，不能整颗粒子以固定不透明度突然出现或消失。
- 同一效果中的粒子必须具有可控的大小、寿命、速度、方向、旋转和明暗分布，避免完全一致。
- 发光核心负责短促高亮，外围粒子负责软边辉光和消散。`emissive` 不能代替透明度渐变；没有全屏 Bloom 时，应使用软边材质、加法混合和局部 HDR 高亮保证可见性。
- 岩石、木屑、金属碎片等有实体体积的碎片可以使用不透明硬边几何体，但仍需通过大小、旋转、速度和明暗变化表现材质差异。
- 优先复用 `src/art/vfxMaterials.js` 的共享软粒子纹理和材质工厂，不要为同类效果重复创建纹理或材质体系。

### 构成与动画节奏

- 一个主要战斗效果通常由两到四层组成：瞬时高亮核心、软边主体、消散粒子、地面范围或落点反馈。
- 爆发和冲击动画必须遵循“先快后慢”：初始帧快速建立力度和范围，随后明显减速、上升、缩小并淡出。不能匀速飘移、突然停止或整块同时消失。
- 粒子的运动方向必须对应攻击、碰撞、爆炸或气流方向；主体、消散粒子与地面反馈必须在正确的触发帧出现。
- 范围伤害的视觉尺寸必须依据实际伤害半径计算，不能把范围爆炸表现成不足一米的单点效果。
- 持续地形效果除主体粒子外，还应有贴合地面的痕迹或半透明氛围层；范围预警需要清楚交代作用区域，并在结束时渐隐而不是瞬间消失。

### 命中、攻击与死亡

- 普通攻击反馈显示在伤害实际结算的击中点或近战接触面，不能显示在攻击者手上，也不能退化为目标脚下坐标。
- 击中特效以克制、短促的向外爆发为主，不需要强烈辉光，但必须有清晰爆发感；不能盖住单位轮廓和伤害数字。
- 击中粒子的透明度 Falloff 必须在较短半径内快速衰减，保留清晰核心，避免大范围柔化成模糊光斑。
- 所有单位死亡都要播放白烟，且按单位体型缩放。白烟由大小、长宽比和旋转不同的半透明低多边形烟块构成，先快速向外爆发，再缓慢向上蒸腾、动态缩小并淡出；禁止用模糊软贴图糊成灰白光斑。
- 爆炸使用一闪而过的 HDR 橙色低多边形核心；外围白烟块快速建立爆炸范围后减速上升、缩小、淡出，不能使用持续扩张的实心圆盘代替。

### 特定元素表现

- 水系效果至少包含透明主体和持续运动的流向信息；水球需要本体旋转、错速环流和绕球水滴，不能只是静止蓝色球体。
- 火焰需要由亮到暗、由实到虚的空间渐变和持续向上的流动；陨石包裹火焰必须动态变化，落地时要有与范围匹配的土石爆发和逐渐消退的范围提示。
- 闪电不能依赖通常无效的 `LineBasicMaterial.linewidth`；使用低边数分段实体、HDR 亮核与外层辉光建立稳定的世界空间宽度。
- 雷云由互相穿插、错速翻涌的低多边形云块构成连续体，闪电在内部短促激荡；地面简影使用适度比例的椭圆，不能过扁或过宽。
- 烟雾和毒雾使用大小不一、层次清楚的半透明低多边形块柔和叠加，避免硬边球体穿插或整体模糊。
- 基地激光沿长度方向保持完整，不做首尾透明渐隐，只在光束两侧做边缘渐隐；消散粒子必须使用软边粒子。
- 训练与兵种专精的升级反馈使用多层软边环形光束绕单位旋转，并用短促星芒交代强化瞬间。

### 渲染层级

- 默认世界特效使用主世界 `layer 0`、开启深度测试、不提高 `renderOrder`，应被单位和场景正常遮挡；祭坛占领环、单位框选/选中标志、驻守圈、死亡白烟、升级环和陨石范围提示均遵循这一规则。
- 地面范围线应略高于地形以避免深度冲突，但不能穿透单位显示，也不能为了补齐断线而增加无意义的采样或强行置顶。
- 透明效果默认关闭深度写入。是否关闭深度测试必须根据辨识需求单独判断，不得无条件覆盖所有单位，也不能让地形完全遮掉关键命中反馈。

### 性能与验收

- 高频特效必须使用对象池，并共享几何体、材质和纹理；禁止在每帧更新或每次命中时无必要地创建、销毁 GPU 资源。
- 为移动端和大量单位同屏预留预算。先解决重复扫描、重复资源创建和透明层过度绘制，再考虑降低更新频率或粒子数量。
- 对象池回收后再次使用时，必须重置旋转、缩放、透明度、颜色、寿命和内部动画状态。
- 必须在实际游戏相机距离和真实战斗场景中验收：效果可见但不喧宾夺主，粒子边缘无硬边纯色色块，尺寸匹配实际作用范围，且不会破坏单位辨识和操作反馈。

## 性能原则

新增功能前先考虑性能预算，保证 RTS 在大量单位、投射物和特效同时存在时仍保持流畅。
优先采用：
- 缓存计算结果，避免每帧重复获取或重复创建对象
- 事件驱动和定时更新，避免每帧全量遍历
- 空间划分或邻域查询，避免无意义的 O(N²) 搜索
- 对重复模型使用共享几何体、共享材质和实例化渲染
- 对远距离或非关键对象降低更新频率
- 复用投射物、粒子和临时对象，减少 GC
- 合理控制阴影、后处理、透明物体和纹理数量

不要用“降低更新频率”掩盖明显的重复扫描、重复寻路或重复资源创建问题。应先定位根因，再优化架构。

## 开发要求

每次修改前先理解现有架构，优先复用已有数据、系统和工具，不要随意重写稳定逻辑。
新增内容必须：
- 保持现有玩法兼容，除非明确要求改动规则
- 有清晰的数据入口和可配置参数
- 不破坏单位、卡牌、寻路、战斗、UI 和存档等已有系统
- 完成基础验证：语法检查、构建检查，并在可能时进行实际游戏验证
- 只修改与当前任务直接相关的文件，不清理、回滚或覆盖用户未提交的改动
- 未经用户明确要求，不得执行 `git commit`、`git push`、创建 Pull Request、部署公网或修改远程仓库设置
- 可以查看 `git status`、`git diff`、分支和远程地址以辅助开发，但任何会改变版本库或远程状态的操作必须先获得用户明确授权

最终目标是制作一款真正可持续扩展、可游玩、性能稳定，并具有完整风格化美术体验的 RTS 卡牌游戏。

## 联机系统与更新日志

**双人合作联机**：规则、协议、目录、改造清单与里程碑见 `docs/COOP_MULTIPLAYER.md`。Host 权威 + 中继转发；双经济（各自牌组/能量/银币/军需铺/波次奖励）+ 共享营地；敌军合作缩放 2.5x 血 / 1.3x 攻、数量不变。联机代码放 `src/network/`、`src/coop/`、`server/`，经 `GameNetworkBridge` 接入，不要往 `Game.js` 堆 WebSocket。

**更新日志（提交与公网部署必做）**
正式更新日志只写在游戏主菜单内，入口在 `src/systems/MetaGameSystem.js` 的 `CHANGELOG_ENTRIES`；不要单独维护 `docs/UPDATE_LOG.md` 作为玩家可见版本。
每次 **git 提交** 或 **更新到公网** 前，必须执行：
1. 对比自上次更新日志以来的实际改动
2. 与 `CHANGELOG_ENTRIES` 最新一条（`[0]`）逐项核对
3. 在 `CHANGELOG_ENTRIES` **顶部**新增一条（或合并进当天条目）
4. **每次更新公网必须递增版本号**：修改 `src/version.js` 的 `GAME_VERSION`（如 `0.2.123 → 0.2.124`）。这是强制项，不是"若对外发布才做"——联机双方靠 `GAME_VERSION` 做版本校验；内容改动但版本不变会让 host 与 client 出现"版本一致但实际内容不同"的错配，导致校验形同虚设、对局不同步。版本号递增后 `TEST_VERSION_LABEL`（`测试版本 v${GAME_VERSION}`）自动跟随，无需单独修改。
5. 完成 `node --check` / `npm run build` 后再提交或部署公网。
